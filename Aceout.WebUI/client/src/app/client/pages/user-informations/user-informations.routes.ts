import { Routes } from '@angular/router';
import { UserInformationsComponent } from './user-informations.component';

export const routes: Routes = [
    {
        path: '', component: UserInformationsComponent,
    },

];
