import { Routes } from '@angular/router';
import { UserPathsComponent } from './user-paths.component';

export const routes: Routes = [
    {
        path: '', component: UserPathsComponent,
    },

];
