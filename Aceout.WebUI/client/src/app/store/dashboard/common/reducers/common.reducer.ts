import * as CommonActions from '../actions';

export interface CommonState {

}


const initialState: CommonState = {

};



export function reducer(state: CommonState = initialState, action: CommonActions.Actions) {
    switch (action.type) {

        default: return {

        }
    }
}
